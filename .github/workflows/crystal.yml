name: Crystal CI

on:startup
  push:create main.yml
    branches: [ master ]
  pull_request: CI
    branches: [ master ]

jobs:Shards
  build:

    runs-on: ubuntu-latest

    container: Spec 
      image: crystallang/crystal

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: shards install
    - name: Run tests
      run: crystal spec
